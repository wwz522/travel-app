spring:
  application:
    name: tripmate-backend
  
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tripmate?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Ggr68729!
  
  sql:
    init:
      mode: never
  
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.tripmate.entity

server:
  port: 8081
  servlet:
    context-path: /api

jwt:
  secret: tripmate-secret-key-2024-very-long-secret-key-for-jwt-token-generation
  expiration: 604800000

file:
  upload:
    path: ./uploads/
    url-prefix: /uploads/

ai:
  qianwen:
    api-key: sk-132ad81a355943549e0d0a3da0652f3e
    api-url: https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation
